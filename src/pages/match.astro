---
import Layout from "../layouts/Layout.astro";

const id = Astro.url.searchParams.get("id");
---

<Layout>
  <main>
    <h1>Match</h1>
    {id ? <p>Viewing match {id}</p> : <p>No match ID provided.</p>}
    <a href="/matches">Back to Matches</a>
  </main>
</Layout>
